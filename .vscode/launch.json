{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run MR Worker",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/src/main/mrworker.go",
            "args": ["${workspaceFolder}/src/mrapps/wc.so"],
            "env": {},
            "cwd": "${workspaceFolder}/src/main",
            "output": "debug"
        }
    ]
}
